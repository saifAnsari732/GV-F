export const metadata = {
  title: 'Login | GV Computer Center',
  description: 'Login to your GV Computer Center account to access your courses, assignments, and dashboard.',
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
