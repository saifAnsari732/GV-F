export const metadata = {
  title: 'Register | GV Computer Center',
  description: 'Create an account to enroll in courses at GV Computer Center.',
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}
